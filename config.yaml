podcast:
  title: "Daily Personal Podcast"
  description: "Curated daily briefing, generated for one listener."
  author: "Your Name"
  language: en-US
  base_url: "https://example.github.io/daily-personal-podcast/"

show:
  target_total_minutes: 25
  narrator:
    name: "Alex"
    persona: "Calm, concise daily briefer."
  host_a:
    name: "Sam"
    persona: "Curious, asks the obvious follow-up."
  host_b:
    name: "Jordan"
    persona: "Skeptical, pushes for what's actually new."

research:
  parallel: true
  max_segments_concurrent: 4
  per_segment_timeout_seconds: 180

tts:
  provider: elevenlabs
  cache: true
  voices:
    narrator: "REPLACE_WITH_ELEVENLABS_VOICE_ID"
    host_a:   "REPLACE_WITH_ELEVENLABS_VOICE_ID"
    host_b:   "REPLACE_WITH_ELEVENLABS_VOICE_ID"
  options:
    elevenlabs:
      model: eleven_turbo_v2_5
      stability: 0.5
      similarity_boost: 0.75

publish:
  output_dir: docs
  recent_window_days: 7
  segment_history_token_cap: 1500
  recent_digest_word_cap: 500
